v4.2.5 — Packaging only

- Switched the per-ABI version code scheme to F-Droid's standard for Flutter apps (base × 10 + ABI). No functional changes whatsoever.
- Same signing key, updates cleanly over any previous install.
