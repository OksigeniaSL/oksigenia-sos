v4.2.3 — Leaner build

- Enabled R8 code and resource shrinking. This removes Flutter's unused Google Play Core (deferred components) classes, which were never used by the app, so the APK is now fully free of Play Store delivery code.
- Smaller APK (~3 MB lighter), no functional changes.
- Validated on device: monitoring, fall detection, the SOS SMS, reboot persistence and the home-screen widgets all work as before.
- Same signing key as always, so this updates cleanly over any previous install.
